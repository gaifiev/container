/* Задание 1
Создать переменные num1, num2 которые пользователь вводит с клавиатуры
Проверьте, что переменная num1 равна или меньше 1, а переменная num2 больше или равна 3 */

const num1 = 1;
const num2 = 4;

if (num1 <= 1 && num2 >= 3) {
    console.log(`числа ${num1} и ${num2} в условиях выполняются`);
} else {
    console.log(`числа ${num1} и ${num2} в условиях не выполняются`);
}
